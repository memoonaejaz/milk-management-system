{
  "name": "milk-backend",
  "version": "1.0.0",
  "description": "Milk Management System Backend using JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
